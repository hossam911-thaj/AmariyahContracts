# 📋 ملخص شامل لمشروع عمارية العهود التجارية

## 🎯 نوع المشروع
مشروع **Full-Stack** متكامل:
- **Frontend**: React 19 مع TypeScript و Vite
- **Backend**: Express.js مع Node.js
- **Database**: PostgreSQL مع Drizzle ORM
- **UI Library**: Radix UI + Tailwind CSS

---

## 📦 البيانات الأساسية

| المعلومة | القيمة |
|---------|--------|
| **اسم المشروع** | rest-express |
| **الإصدار** | 1.0.0 |
| **الترخيص** | MIT |
| **نوع الملحة** | ESM (ES Modules) |
| **لغة** | TypeScript 5.6.3 |
| **إصدار Node** | 20+ |
| **إصدار React** | 19.2.0 |

---

## 🚀 أوامر التشغيل المهمة

### تثبيت وإعداد
```bash
# تثبيت المتعلقات
npm install

# فحص أخطاء TypeScript
npm run check

# دفع تحديثات قاعدة البيانات
npm run db:push
```

### التطوير
```bash
# تطوير السيرفر (Backend) - على Port 5000
npm run dev

# تطوير العميل (Frontend) - على Port 5000
npm run dev:client

# ملاحظة: يمكن تشغيل الأمرين في نفس الوقت في terminals منفصلة
```

### الإنتاج
```bash
# بناء المشروع للإنتاج
npm run build

# تشغيل الإنتاج
npm run start
```

---

## 🗄️ متطلبات قاعدة البيانات

### متغير البيئة المطلوب
```
DATABASE_URL=postgresql://user:password@localhost:5432/database_name
```

### الجداول المعرفة

#### جدول Users
```typescript
users {
  id: varchar (UUID) - المفتاح الأساسي
  username: text - فريد (unique)
  password: text
}
```

### عمليات Drizzle
```bash
# إنشاء/تحديث الجداول في قاعدة البيانات
npm run db:push

# عرض الهجرات
ls migrations/
```

---

## 📁 هيكل المشروع

```
AmariyahContracts/
│
├── client/                          # تطبيق React الأمامي
│   ├── src/
│   │   ├── components/              # مكونات React المعاد استخدامها
│   │   │   └── ui/                  # مكونات UI من Radix
│   │   ├── pages/                   # صفحات التطبيق
│   │   │   ├── home.tsx
│   │   │   └── not-found.tsx
│   │   ├── hooks/                   # React hooks مخصصة
│   │   │   ├── use-mobile.tsx
│   │   │   └── use-toast.ts
│   │   ├── lib/                     # وسائل ودوال مساعدة
│   │   │   ├── language-context.tsx # دعم تعدد اللغات
│   │   │   ├── pdf-generator.ts
│   │   │   ├── queryClient.ts
│   │   │   └── utils.ts
│   │   ├── App.tsx                  # مكون التطبيق الرئيسي
│   │   ├── main.tsx                 # نقطة الدخول
│   │   └── index.css                # التصاميم العام
│   ├── index.html                   # ملف HTML الرئيسي
│   └── public/                      # أصول عام
│
├── server/                          # سيرفر Express
│   ├── index.ts                     # نقطة الدخول وإعداد التطبيق
│   ├── routes.ts                    # تعريف مسارات API
│   ├── storage.ts                   # طبقة البيانات والـ CRUD
│   └── vite.ts                      # إعداد Vite في بيئة التطوير
│
├── shared/                          # كود مشترك بين العميل والسيرفر
│   └── schema.ts                    # تعريفات Zod و Drizzle للبيانات
│
├── script/                          # سكريبتات بناء مخصصة
│   └── build.ts                     # سكريبت البناء للإنتاج
│
├── attached_assets/                 # الصور والملفات المرفقة
│   └── stock_images/
│
├── migrations/                      # تتم إنشاؤها تلقائياً - هجرات Drizzle
│
├── dist/                            # تتم إنشاؤها تلقائياً - مخرجات البناء
│   └── public/                      # الملفات الثابتة المُبنية
│
├── node_modules/                    # تتم إنشاؤها تلقائياً - المتعلقات
│
├── .env                             # متغيرات البيئة (لم ينشر في Git)
├── .env.example                     # مثال على متغيرات البيئة
├── .gitignore                       # الملفات المستبعدة من Git
├── .replit                          # إعدادات Replit
│
├── tsconfig.json                    # إعدادات TypeScript
├── vite.config.ts                   # إعدادات Vite
├── drizzle.config.ts                # إعدادات Drizzle ORM
├── postcss.config.js                # إعدادات PostCSS
├── components.json                  # إعدادات مكونات shadcn/ui
├── tailwind.config.js               # إعدادات Tailwind CSS
│
└── package.json                     # معلومات المشروع والمتعلقات
```

---

## 🔧 المتعلقات الرئيسية

### Frontend (React & UI)
- `react@19.2.0` - مكتبة React
- `react-dom@19.2.0` - DOM rendering
- `@radix-ui/*` - مكونات UI عالية الجودة (30+ مكون)
- `tailwindcss@4.1.14` - CSS utility framework
- `@tanstack/react-query@5.60.5` - إدارة حالة البيانات
- `wouter@3.3.5` - راوتر lightweight
- `react-hook-form@7.66.0` - إدارة النماذج
- `zod@3.25.76` - التحقق من البيانات
- `framer-motion@12.23.24` - الحركات والرسومات المتحركة
- `recharts@2.15.4` - رسوم بيانية

### Backend (Server)
- `express@4.21.2` - إطار عمل الويب
- `passport@0.7.0` - المصادقة
- `passport-local@1.0.0` - إستراتيجية المصادقة المحلية
- `express-session@1.18.1` - إدارة الجلسات
- `connect-pg-simple@10.0.0` - تخزين الجلسات في PostgreSQL
- `ws@8.18.0` - WebSocket

### Database
- `postgres@8.16.3` - سائق PostgreSQL
- `drizzle-orm@0.39.3` - ORM TypeScript
- `drizzle-kit@0.31.4` - أدوات وهجرات Drizzle
- `drizzle-zod@0.7.0` - دعم Zod في Drizzle

### أدوات
- `typescript@5.6.3` - TypeScript
- `vite@7.1.9` - bundler متقدم
- `@vitejs/plugin-react@5.0.4` - دعم React في Vite
- `@tailwindcss/vite@4.1.14` - دعم Tailwind في Vite
- `esbuild@0.25.0` - bundler سريع للسيرفر
- `tsx@4.20.5` - تنفيذ TypeScript

---

## ⚙️ الإعدادات المهمة

### TypeScript Path Aliases
```json
{
  "paths": {
    "@/*": ["./client/src/*"],
    "@shared/*": ["./shared/*"],
    "@assets/*": ["./attached_assets/*"]
  }
}
```

### Vite Configuration
- **Root**: `client/` (مجلد React)
- **Build Output**: `dist/public/`
- **Server Host**: `0.0.0.0`
- **Development Server**: Hot Module Reloading مفعل
- **Plugins**: React، Tailwind، Runtime Error Modal

### Environment Files
```
.env                # متغيرات الإنتاج (private)
.env.example        # مثال للإعدادات (يتم نشره)
```

### Ports (من .replit)
- **Local Port**: 5000
- **External Port**: 80 (Replit)
- **Server Host**: 0.0.0.0

---

## 🔑 خطوات التشغيل الكاملة

### 1. التثبيت الأولي
```bash
npm install
```

### 2. إعداد متغيرات البيئة
```bash
# انسخ المثال وعدّل البيانات
cp .env.example .env

# ثم عدّل .env بمعلومات قاعدة البيانات الفعلية
```

### 3. إعداد قاعدة البيانات
```bash
# تأكد من تشغيل PostgreSQL أولاً
npm run db:push
```

### 4. التطوير
```bash
# في Terminal 1: السيرفر
npm run dev

# في Terminal 2: العميل (اختياري، يمكن تشغيله من نفس العملية)
npm run dev:client
```

### 5. الوصول للتطبيق
```
http://localhost:5000
```

### 6. البناء للإنتاج
```bash
npm run build
```

### 7. تشغيل الإنتاج
```bash
npm run start
```

---

## 🌐 دعم اللغات

المشروع يدعم اللغات المتعددة:
- **العربية**: الافتراضية مع RTL
- **الإنجليزية**: اختيارية

يتم التحكم من خلال:
- `LanguageProvider` في React
- `language-context.tsx`

---

## 🔐 الأمان

### المصادقة
- Passport.js مع LocalStrategy
- تخزين الجلسات في PostgreSQL
- express-session للإدارة

### التحقق من البيانات
- Zod للتحقق من البيانات من الطرف الأمامي والخلفي
- drizzle-zod لتوليد الـ schemas

---

## 📝 ملفات مهمة

| الملف | الوصف |
|------|-------|
| `package.json` | معلومات المشروع والسكريبتات والمتعلقات |
| `tsconfig.json` | إعدادات TypeScript |
| `vite.config.ts` | إعدادات Vite والبناء |
| `drizzle.config.ts` | إعدادات Drizzle ORM والهجرات |
| `shared/schema.ts` | تعريفات البيانات والـ schemas |
| `.env` | متغيرات البيئة (يجب إنشاؤها) |
| `.replit` | إعدادات Replit |

---

## ⚠️ ملاحظات مهمة

✅ **لا توجد ملفات `.env`** - يجب إنشاء واحدة قبل التشغيل  
✅ **PostgreSQL مطلوب** - يجب أن يكون قيد التشغيل  
✅ **Node.js 20+** - الإصدار المطلوب  
✅ **npm 10+** - مدير المتعلقات  
✅ **نظام دعم RTL كامل** - HTML بـ `lang="ar"` و `dir="rtl"`  
✅ **بناء محسّن** - مع esbuild لتقليل وقت بدء التطبيق  
✅ **Hot Reload** - في بيئة التطوير للتطوير السريع  

---

## 🐛 استكشاف الأخطاء

### الخطأ: `DATABASE_URL not found`
**الحل**: تأكد من وجود `.env` وأنه يحتوي على `DATABASE_URL`

### الخطأ: `Cannot connect to PostgreSQL`
**الحل**: تأكد من تشغيل PostgreSQL وصحة بيانات الاتصال

### الخطأ: `Port 5000 already in use`
**الحل**: غيّر المنفذ بإضافة `PORT=3000` في `.env`

### الخطأ: `node_modules missing`
**الحل**: شغّل `npm install`

---

## 📚 المراجع

- [React Documentation](https://react.dev)
- [Vite Guide](https://vitejs.dev)
- [Drizzle ORM](https://orm.drizzle.team)
- [Radix UI](https://www.radix-ui.com)
- [Tailwind CSS](https://tailwindcss.com)
- [Express.js](https://expressjs.com)

---

## 👥 فريق المشروع

**المشروع**: عمارية العهود التجارية  
**النوع**: Full-Stack Web Application  
**الحالة**: تحت التطوير  

---

**آخر تحديث**: 7 ديسمبر 2025
